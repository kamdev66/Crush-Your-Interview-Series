import 'package:flutter/material.dart';

class Widget_1 extends StatelessWidget {
  const Widget_1({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Algorithm Baba"),
      ),
    );
  }
}