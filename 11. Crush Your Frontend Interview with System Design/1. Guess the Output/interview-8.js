// GUESS THE OUTPUT ??

console.log(   5 + "2" );  
console.log( "5" + "2" );

console.log(   5 - "2" );
console.log( "5" - "2" );
console.log( "A" - "A" );

// Javascript '+' operator behaves as:-,
// number operator when both operands are number
// concat operator if any one or both of the operands are string.

// Javascript '-' operator always behaves as:-
// number operator:- if any one or both of the operands are string, 
// Javascript attempts to convert it to a number, if not able to convert
//  then return NaN.